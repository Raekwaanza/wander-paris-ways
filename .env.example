# Public, non-sensitive provider/configuration selection. Supported values:
# mock, live. The current hybrid provider set always attempts server-backed
# MapTiler/ORS operations when their server keys exist and otherwise uses
# seeded search or Preview routes. "live" remains reserved for a future fully
# live provider set; it currently warns and reports the hybrid set as active.
VITE_SCENIC_DATA_MODE=mock

# Public HTTPS origin used by bundled iOS/Android clients for /api/v1 requests.
# Supply an origin only (no path/query/fragment). This is not a provider
# credential and must not contain ORS or MapTiler keys.
# VITE_SCENIC_API_BASE_URL=https://api.example.com

# Canonical public HTTPS origin used in route-share links from web and native
# clients. Supply an origin only (no path/query/fragment). This is intentionally
# separate from the API origin even when both currently point to one Worker.
# VITE_SCENIC_PUBLIC_WEB_ORIGIN=https://scenic-route.example.workers.dev

# Optional server-only comma-separated additions to the native API CORS
# allowlist. Defaults already include Capacitor's localhost WebView origins.
# Origin allowlisting is a browser control, not client authentication.
# SCENIC_NATIVE_ALLOWED_ORIGINS=https://localhost,capacitor://localhost

# Server-only credential used for Paris forward search and best-effort reverse
# label enrichment of browser GPS fixes.
MAPTILER_API_KEY=

# Server-only credential for ORS foot-walking directions, alternatives, and
# Wander Matrix/via requests. When unavailable, product routes safely fall
# back to synthetic Preview geometry.
OPENROUTESERVICE_API_KEY=

# Public MapLibre style URL. Defaults to OpenFreeMap Liberty when omitted.
# VITE_SCENIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
